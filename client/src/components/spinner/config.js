// Tunables for the spinner animation. Surfaced in one place so 007 can
// expose them as user-overridable settings later.

export const SPIN_DURATION_MS = 2000;
export const FULL_ROTATIONS = 4;
export const MAX_VISIBLE_SLOTS = 12;
