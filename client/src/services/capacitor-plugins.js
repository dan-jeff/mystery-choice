// Re-export point for app-defined Capacitor plugins. Later features add:
//   GameDetector (002), Launcher (005), Purchases (010).
// Keep this file as the single import surface so feature code never imports
// from @capacitor/core directly.

export {};
